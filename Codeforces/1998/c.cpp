#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int tt; cin >> tt;
    while (tt--) {
        int n, k; cin >> n >> k;
        vector<int> a(n);
        vector<int> b(n);
        for (int& e : a) cin >> e;
        for (int& e : b) cin >> e;
        multiset<int> ms;
        auto med_it = ms.begin();
        int med_idx = 1;
        
    }
    return 0;
}
