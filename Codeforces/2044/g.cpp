#include <bits/stdc++.h>
using namespace std;
const int maxn = 2e5 + 5;

void solve() {
    int n; cin >> n;
    vector<int> r(n);

    for (int& e : r) cin >> e;
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int tt;
    cin >> tt;
    while (tt--) {
        solve();
    }
    return 0;
}

